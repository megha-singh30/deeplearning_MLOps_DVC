## This file contains constants that are used throughout the CNN classifier project.
## This helps to avoid hardcoding values in multiple places and makes it easier to manage 
# and update

from pathlib import Path

CONFIG_FILE_PATH = Path("config/config.yaml")
PARAMS_FILE_PATH = Path("params.yaml")